<%= form_for([@project, @edition], :html => {:class => :basic}) do |f| %>
  <% if @edition.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@edition.errors.count, "error") %> prohibited this edition from being saved:</h2>

      <ul>
      <% @edition.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <p class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p class="field">
    <%= f.label :intro %><br />
    <%= f.text_area :intro %>
  </p>
  <p class="actions">
    <%= f.submit %>
  </p>
<% end %>
